import Button from "@/app/components/button";
import Header from "@/app/components/header";

export default function CustomClasses() {
  return (
    <div>
      <Header level="h1" text="Custom Classes & Themes" />
      <Button title="Primary Button" size="large" />
    </div>
  );
}
